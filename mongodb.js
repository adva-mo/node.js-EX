// show dbs =>
//  use first-test =>                               navigate to 'first-set' db, if there isnt, a new will created
//  db.users.insertOne({name:"max",age:29}) =>      will create new document objet in users collection
//  show collections =>                             displays all collections in db
//  db.users.find() =>                              will findl all the documents
//  cls =>                                          clear command
//  db.users.insertMany([{},{}])                    will insert more thrn one object to thecollection, need to be an array of object
//  db.users.find({age:26}) =>                            get the documents whuch fits the creteria provided in {}
